# 阅小师 - 大学物理AI智能批改系统

## 后端需求分析文档

---

## 一、系统概述

| 项目 | 内容 |
|------|------|
| **系统名称** | 阅小师 - 大学物理AI智能批改系统 |
| **系统类型** | B/S架构 Web应用 |
| **核心功能** | 题库管理、作业批改、学情分析、错题复盘、成长报告 |
| **用户角色** | 教师、学生 |

---

## 二、角色与权限

### 2.1 教师端功能

- **题库管理**：校本/云端题库切换、题目CRUD、筛选搜索、AI智能组卷
- **作业管理**：发布作业、查看提交进度、OCR扫描录入、AI/人工批改
- **学情分析**：班级知识点掌握度、AI教学洞察、班级活跃度统计
- **个人中心**：账号信息、通知偏好、安全设置

### 2.2 学生端功能

- **作业完成**：查看待完成作业、开始答题、提交作业
- **错题本**：错题分类、AI复练推荐、查看解析
- **成长报告**：学习时长、进步追踪、荣誉勋章
- **个人中心**：学习数据、账号安全

---

## 三、核心API接口设计

### 3.1 认证模块 `/api/auth`

| 接口 | 方法 | 功能 |
|------|------|------|
| `/login` | POST | 用户登录（教师/学生） |
| `/register` | POST | 用户注册 |
| `/logout` | POST | 退出登录 |
| `/refresh-token` | POST | 刷新Token |

### 3.2 题库模块 `/api/questions`

| 接口 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 获取题库列表（支持分页、筛选） |
| `/local` | GET | 获取校本题库 |
| `/cloud` | GET | 获取云端题库 |
| `/:id` | GET | 获取题目详情 |
| `/` | POST | 创建题目（教师） |
| `/:id` | PUT | 更新题目（教师） |
| `/:id` | DELETE | 删除题目（教师） |
| `/generate` | POST | **AI生成题目** |

**AI生成题目请求体**：

```json
{
  "chapter": "牛顿力学",
  "difficulty": "中等",
  "count": 5,
  "type": "计算题"
}
```

### 3.3 作业模块 `/api/assignments`

| 接口 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 获取作业列表（教师/学生） |
| `/:id` | GET | 获取作业详情 |
| `/` | POST | 创建作业（教师） |
| `/:id` | PUT | 更新作业（教师） |
| `/:id/submit` | POST | 提交作业（学生） |
| `/:id/grade` | POST | **AI批改作业** |

**AI批改请求体**：

```json
{
  "assignmentId": "xxx",
  "studentAnswer": "xxx",
  "questionId": "xxx"
}
```

### 3.4 学情分析模块 `/api/analytics`

| 接口 | 方法 | 功能 |
|------|------|------|
| `/class/:classId` | GET | 获取班级学习分析 |
| `/knowledge-mastery` | GET | 获取知识点掌握度 |
| `/ai-insight` | GET | **AI教学洞察** |
| `/class-activity` | GET | 班级活跃度数据 |

### 3.5 错题本模块 `/api/error-questions`

| 接口 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 获取学生错题列表 |
| `/:id` | GET | 获取错题详情 |
| `/:id/practice` | POST | **AI推荐复练题目** |

### 3.6 成长报告模块 `/api/growth`

| 接口 | method | 功能 |
|------|--------|------|
| `/report/:studentId` | GET | 获取学生成长报告 |
| `/weekly-stats` | GET | 每周学习时长统计 |
| `/achievements` | GET | 荣誉勋章成就 |

### 3.7 个人中心 `/api/profile`

| 接口 | method | 功能 |
|------|--------|------|
| `/` | GET | 获取个人信息 |
| `/` | PUT | 更新个人信息 |
| `/password` | PUT | 修改密码 |
| `/notifications` | GET/PUT | 通知偏好设置 |

---

## 四、AI能力需求

### 4.1 AI题目生成

- **输入**：章节名称、难度、题型、数量
- **输出**：符合教学要求的物理题目（含答案）
- **技术要点**：大语言模型调用、物理学公式校验

### 4.2 AI作业批改

- **输入**：学生作答内容、题目标准答案
- **输出**：得分、错误点标注、得分理由
- **技术要点**：公式识别（OCR）、语义理解、分步给分

### 4.3 AI学情分析

- **输入**：班级作业数据、学生历史表现
- **输出**：知识点薄弱点预警、优生推荐、教学建议
- **技术要点**：数据分析、模式识别

### 4.4 AI错题复练

- **输入**：错题类型、学生薄弱点
- **输出**：针对性练习题目推送
- **技术要点**：知识图谱、个性化推荐

### 4.5 AI成长洞察

- **输入**：学生学习行为数据
- **输出**：进步分析、学习建议、成就解锁
- **技术要点**：时序数据分析

---

## 五、数据模型设计

### 5.1 用户表 `users`

```sql
CREATE TABLE users (
  id VARCHAR(36) PRIMARY KEY,
  email VARCHAR(100) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  role ENUM('teacher', 'student') NOT NULL,
  name VARCHAR(50) NOT NULL,
  avatar VARCHAR(255),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 5.2 教师扩展表 `teachers`

```sql
CREATE TABLE teachers (
  user_id VARCHAR(36) PRIMARY KEY REFERENCES users(id),
  department VARCHAR(100),      -- 院系
  title VARCHAR(50),            -- 职称
  is_verified BOOLEAN DEFAULT FALSE,
  id_card VARCHAR(50)           -- 教职工号
);
```

### 5.3 学生扩展表 `students`

```sql
CREATE TABLE students (
  user_id VARCHAR(36) PRIMARY KEY REFERENCES users(id),
  student_id VARCHAR(50) UNIQUE,  -- 学号
  class_name VARCHAR(100),         -- 班级
  grade VARCHAR(20),              -- 年级
  physics_level VARCHAR(20) DEFAULT 'beginner'  -- Physics Master等级
);
```

### 5.4 题目表 `questions`

```sql
CREATE TABLE questions (
  id VARCHAR(36) PRIMARY KEY,
  source ENUM('local', 'cloud') DEFAULT 'local',
  title TEXT NOT NULL,
  type ENUM('选择题', '填空题', '计算题', '简答题') NOT NULL,
  difficulty ENUM('简单', '中等', '困难') NOT NULL,
  chapter VARCHAR(50) NOT NULL,     -- 章节：力学、热学、电磁学、光学、近代物理
  content JSON NOT NULL,            -- 题目内容（含图片）
  answer JSON NOT NULL,             -- 标准答案
  analysis TEXT,                     -- 题目解析
  created_by VARCHAR(36) REFERENCES users(id),
  used_count INT DEFAULT 0,
  is_verified BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 5.5 作业表 `assignments`

```sql
CREATE TABLE assignments (
  id VARCHAR(36) PRIMARY KEY,
  title VARCHAR(200) NOT NULL,
  teacher_id VARCHAR(36) REFERENCES users(id),
  class_name VARCHAR(100) NOT NULL,
  deadline DATETIME NOT NULL,
  grading_type ENUM('AI', '人工+AI') DEFAULT 'AI',
  status ENUM('draft', 'active', 'finished', 'graded') DEFAULT 'draft',
  question_ids JSON NOT NULL,       -- 作业包含的题目ID列表
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 5.6 作业提交表 `submissions`

```sql
CREATE TABLE submissions (
  id VARCHAR(36) PRIMARY KEY,
  assignment_id VARCHAR(36) REFERENCES assignments(id),
  student_id VARCHAR(36) REFERENCES users(id),
  answers JSON NOT NULL,            -- 学生答案
  score DECIMAL(5,2),                -- 总分
  ai_feedback JSON,                  -- AI批改反馈
  status ENUM('submitted', 'graded') DEFAULT 'submitted',
  submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  graded_at TIMESTAMP
);
```

### 5.7 错题表 `error_questions`

```sql
CREATE TABLE error_questions (
  id VARCHAR(36) PRIMARY KEY,
  student_id VARCHAR(36) REFERENCES users(id),
  question_id VARCHAR(36) REFERENCES questions(id),
  error_type VARCHAR(50),            -- 概念模糊、计算失误、公式记错等
  error_count INT DEFAULT 1,
  last_reviewed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 5.8 学习记录表 `learning_records`

```sql
CREATE TABLE learning_records (
  id VARCHAR(36) PRIMARY KEY,
  student_id VARCHAR(36) REFERENCES users(id),
  action_type VARCHAR(50),           -- homework, error_review, practice
  duration_minutes INT,              -- 学习时长
  knowledge_points JSON,             -- 涉及的知识点
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 5.9 成就表 `achievements`

```sql
CREATE TABLE achievements (
  id VARCHAR(36) PRIMARY KEY,
  name VARCHAR(50) NOT NULL,
  description TEXT,
  icon VARCHAR(50),
  criteria JSON
);

CREATE TABLE student_achievements (
  student_id VARCHAR(36) REFERENCES users(id),
  achievement_id VARCHAR(36) REFERENCES achievements(id),
  earned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (student_id, achievement_id)
);
```

---

## 六、技术栈建议

| 层级 | 技术选型 |
|------|----------|
| **后端框架** | Node.js + Express / NestJS 或 Python + FastAPI |
| **数据库** | PostgreSQL + Redis（缓存） |
| **AI服务** | OpenAI API / 文心一言 / 通义千问（LLM调用） |
| **文件存储** | 阿里云OSS / 七牛云（图片、PDF） |
| **OCR识别** | 阿里云OCR / 腾讯云OCR |
| **实时通信** | WebSocket（作业批改进度推送） |
| **认证** | JWT + Refresh Token |

---

## 七、安全需求

1. **身份认证**：JWT Token，支持 Refresh Token 续期
2. **权限控制**：教师与学生接口隔离，作业归属验证
3. **数据安全**：密码加盐哈希传输，敏感数据加密存储
4. **API防护**：请求频率限制、SQL注入防护、XSS防护

---

## 八、非功能性需求

| 需求 | 说明 |
|------|------|
| **性能** | API响应时间 < 500ms，AI批改 < 3s |
| **可用性** | 系统可用性 > 99.5% |
| **扩展性** | 支持未来扩展至其他学科 |
| **兼容性** | 支持主流浏览器（Chrome、Firefox、Safari、Edge） |

---

*文档生成时间：2026-04-23*
